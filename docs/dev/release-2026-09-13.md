# September 13 release verification

This release includes pattern copy/paste and doubling, timed block follow actions and section launches, linear WAV and individual-pattern MIDI exports, visualizer MP4 export, desktop layout refinements, phone help, and track-aware accessible control names.

- Unit suite: 101 files, 1,845 tests passed.
- Targeted Chromium coverage: 23 tests across 11 files. The initial combined run passed 21 tests; the two register-control tests were updated for current labels, the 16-voice drum grid, and explicit phone Help, then both passed on rerun. No app code changed between those browser runs.
- Browser coverage includes the four complete editing/export/reload journeys, audio export and failure recovery, MP4 encoding/decoding, pattern rail, and register controls.
- Full ESLint, TypeScript no-emit, production build, bundle budget, and diff whitespace checks passed.
- The complete repository browser suite was not run as part of this release check.

The existing desktop/phone verification screenshots and reports are retained with their corresponding feature documentation. The pattern export check supports BITBOUNCE_URL and BITBOUNCE_ARTIFACT_DIR for production smoke tests without modifying committed evidence.
