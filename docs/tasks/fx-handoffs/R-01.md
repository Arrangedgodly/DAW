# R-01 FX research handoff

## Status

Complete. Research report only. No production edits, commits, PRs, timed benchmarks, or audition.

## Completed work

- Audited filter, drive, bitcrusher, delay, reverb, chain host, eight-band EQ, compressor, limiter, and live/offline wiring. Full findings and risk-ranked slices are in `docs/dev/fx-performance-audit.md`.
- Separated startup/bundle latency, UI/control cost, offline throughput, live rendering load, and hardware output latency.
- Kept P-02 ownership clear: its reverb size comparison and delay `syncBpm` stale `timeSteps` fixes are documented but not repeated here.

## Evidence and source links

- Local: `src/audio/fx.ts`, `src/audio/mixer.ts`, `src/audio/worklets/voiceEngine.js`, `src/audio/worklets/mixerLimiter.js`, `src/audio/voiceEngine.ts`, `src/audio/render.ts`, `src/audio/context.ts`, `src/engine/session.ts`, `src/document/fx.ts`, `src/document/mixer.ts`, `src/components/MixerPage.tsx`.
- Official: [AudioWorklet process](https://developer.mozilla.org/en-US/docs/Web/API/AudioWorkletProcessor/process), [Convolver normalization](https://developer.mozilla.org/en-US/docs/Web/API/ConvolverNode/normalize), [WaveShaper oversampling](https://developer.mozilla.org/en-US/docs/Web/API/WaveShaperNode/oversample), [BiquadFilterNode](https://developer.mozilla.org/en-US/docs/Web/API/BiquadFilterNode), [Worklet.addModule](https://developer.mozilla.org/en-US/docs/Web/API/Worklet/addModule), [OfflineAudioContext rendering](https://developer.mozilla.org/en-US/docs/Web/API/OfflineAudioContext/startRendering), [AudioContext latency options](https://developer.mozilla.org/en-US/docs/Web/API/AudioContext/AudioContext), [Web Audio latency definitions](https://webaudio.github.io/web-audio-api/#dom-audiocontext-baselatency).
- Adjacent uncommitted benchmark note: `docs/dev/audio-engine-benchmark.md`. Its reported Elementary offline timing has an initial-block mismatch; it cannot yet support an engine change or live latency claim. I did not edit or rerun it.

## Recommended next PRs

1. UI/control: reuse `Session.readMixerSpectrum` scratch storage and check mixer meter parity and allocation behavior.
2. FX host: skip graph edge teardown for parameter-only chain edits while retaining bypass/structural semantics. Test every device, reorder, and live/offline parity.
3. Mixer graph: measure default eight-slot EQ and compressor-node setup/load, then prototype lazy or disconnected disabled paths only if the cost is material. Require click, phase, state, and export parity tests.
4. Compressor: profile enabled worklet processing before a narrow arithmetic change; retain knee, timing, stereo link, and reduction behavior.
5. Reverb: after P-02, measure IR generation on edits before considering a bounded seed/size/sample-rate cache.

## What remains and blockers

- Device playback profiling, listening, browser coverage, and physical output-latency checks remain. This research thread did not run timed benchmarks while other workers benchmarked.
- The Elementary offline comparison has a first-block alignment defect and no valid full-chain parity result. Fix that harness before interpreting its throughput difference; do not presume replacement of the native engine.
- No source-only observation quantifies a speedup. The suggested PRs need isolated measurement and narrow tests before claiming one.

## Exact owned files

- `C:\Users\arran\Projects\DAW\docs\dev\fx-performance-audit.md`
- `C:\Users\arran\Projects\DAW\docs\tasks\fx-handoffs\R-01.md`
