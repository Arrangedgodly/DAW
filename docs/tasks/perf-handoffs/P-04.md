# P-04 handoff: parameter-only FX chain edits

Status: implemented, verified in focused unit and Chromium checks, committed, pushed, and open for review as [PR 4](https://github.com/Arrangedgodly/DAW/pull/4). The PR targets `perf/fx-control-cost` (PR 1), so review and merge it after PR 1.

## Checkout and changes

- Isolated checkout: `C:\Users\arran\Projects\DAW-perf-fx-relink`.
- Branch: `perf/fx-param-relink`; stacked base: `perf/fx-control-cost` at `160aa8b5863f8345c9e88aa1f36d587fb0f80033`.
- Implementation commit: `6a3fa7e33ba2c505050765526e112ae5e4c03e4e`.
- `src/audio/fx.ts`: snapshot bypass and scalar parameter values for each wired device. Same-type parameter edits now call `setParams` for changed values without touching host-owned edges; this detects both new objects and in-place mutation. Bypass changes alone tear down and reconnect the active route. A reference-preserving reorder between same-type slots triggers a structural rebuild. Existing device instances, seeds, ramp constants, delay/reverb internals, and disposal remain on their established paths.
- `tests/fx-chain-host.test.ts`: count connects/disconnects, assert exact route after bypass, verify parameter forwarding for all five device types, cover in-place changes and same-type reorder.
- `tests/browser/fx-graph.test.ts`: check real Chromium offline output after parameter edits for filter, drive, bitcrusher, delay, and reverb. Delay changes are applied at an offline suspension point and checked by echo interval because its 15 ms glide intentionally differs from a fresh graph. A live `AudioContext` graph check watches real host-owned output connections during edits to all five devices.

No document schema, reverb IR generation, delay tempo-sync implementation, or P-02 behavior changed. Generated `dist` files are ignored and absent from the PR.

## Acceptance evidence

- Two successive five-device parameter edits: zero additional host-owned connects and zero disconnects on this branch; every device receives two parameter pushes with the final values. The instance set and route stay intact, with no fade or structural rebuild.
- Baseline proof on `160aa8b`: the new regression tests fail. The same two edits add 12 connects and 12 disconnects, and an in-place parameter edit is not forwarded. The implementation was restored after this baseline run.
- Bypass toggling disconnects the previous four-edge route and connects the intended three-edge route around the bypassed filter. The fake graph asserts exactly one ramp-to-device route and no parallel ramp-to-sink dry route. The bypassed instance remains alive. Re-enabling restores the ordered route. Add/remove/type reorder tests and the new same-type reference reorder test prove rebuilds and disposal; existing tests retain the 3 ms down / 8 ms up fade assertions.
- Chromium offline renders pass with finite, non-silent output for all five effects and preserve reverb tail coverage. The new live `AudioContext` check observes zero host-owned graph mutations for parameter edits. That live graph check mutes its output and is not a listening audition.

## Exact verification

Run sequentially in the isolated checkout with its `node_modules` junction to the original project's installed dependencies:

1. `.\node_modules\.bin\vitest.cmd run --project unit tests/fx-chain-host.test.ts tests/fx.test.ts` — passed, 2 files / 33 tests.
2. `.\node_modules\.bin\vitest.cmd run --project browser tests/browser/fx-graph.test.ts` — passed, 1 file / 14 Chromium tests.
3. `npm run typecheck` — passed.
4. `.\node_modules\.bin\eslint.cmd src/audio/fx.ts tests/fx-chain-host.test.ts tests/browser/fx-graph.test.ts` — passed.
5. `npm run build` — passed. Vite emitted its existing ineffective dynamic import notices for `newProject.ts` and `boot.ts`.
6. `git diff --check` — passed.

Baseline regression command: after temporarily stashing only `src/audio/fx.ts`, `.\node_modules\.bin\vitest.cmd run --project unit tests/fx-chain-host.test.ts -t 'does not mutate host edges|detects in-place parameter'` — failed as expected, 2 tests. Restored the source with `git stash pop`; the full focused checks above passed afterward.

No uncontended timed benchmark was run, so no control-path speedup percentage is claimed. No listening audition, target-device load test, or hardware output-latency measurement was performed; graph mutation counts do not establish hardware latency.

## PR and handoff

- PR URL: https://github.com/Arrangedgodly/DAW/pull/4
- PR base/head verified on GitHub: `perf/fx-control-cost` / `perf/fx-param-relink`; open and mergeable when checked.
- Linked to P-04 T3 thread `57102083-96ca-4b11-9bc7-46a048faf0b2` by `t3-thread.mjs link-pr`; acknowledgment was accepted and `get-thread` listed PR 4.
- Implementation blockers: none. Remote Cloudflare verification is unresolved below. Reference-preserving same-type reorder is supported because the app's `reorderChain` retains device objects; an unrelated caller that clones every same-type device while reordering cannot communicate that intent to this host without stable device IDs. No such app path was found in this slice.
- Next: manager can review PR 4 after PR 1. P-03 and R-03 do not depend on this PR.

## Remote check status at handoff

`gh pr checks 4` reported the Cloudflare Workers build as failed, with only a dashboard URL and no diagnostic text in the GitHub check listing. GitHub CI, browser, and browser-native jobs were still pending. PR 1 had already shown the same opaque Cloudflare failure in the manager's orchestration notes, but the cause of this PR's failure has not been verified. The focused local checks above are green; the complete remote suite is not yet green and requires manager follow-up.
