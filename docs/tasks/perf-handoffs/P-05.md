# P-05 handoff — cold Play repeat and no-change result

**Status:** Complete as an attribution/no-change PR. The attempted production ordering change failed the required median improvement and was reverted. P-06's uncontended timing slot was released in a manager-thread message accepted as message `4314ee03-2c1e-43c4-a88d-5da5ecc4bcc9`.

**Branch and stack:** `perf/cold-start` from `perf/latency-map` at `a292f3d733cd32e65fbb0dffb20afdd78686c6ed`. Evidence commit `4a4e84435402f0736b69c178d8446f95f07c6c5e`. PR [#6](https://github.com/Arrangedgodly/DAW/pull/6) is open against `perf/latency-map` and linked to worker thread `505f1b09-1c84-4ab2-bf6f-18baf8f1db97`; `get-thread` returned the linked PR. No original DAW checkout, shared dependency, manager ledger, scheduler horizon, FX host, spectrum region, document schema, preset, or sample content was edited.

## Work and decision

Read `docs/dev/latency-map.md`, `docs/dev/latency-map-samples.json`, and the Session, context, voice-engine, and mixer startup paths. Added an independent serial probe at `tests/perf/cold-start.mjs`, four raw datasets under `docs/dev/`, and [the attribution report](../../dev/cold-start.md). Every pass uses a new Chromium browser context and page, the Welcome demo, a cold Play and then a warm Play. The capture boundary is the Play click to the first nonempty worklet `events` port post. Resume, module loads, sample fetches/decodes, post batches, and errors are retained as raw browser timestamps. Built assets were checked separately through Vite preview.

The one tested candidate invoked the existing memoized voice-engine preparation while the gesture-started `resume()` promise was pending. The voice module began earlier but its observed load interval lengthened. The candidate was reverted; the final PR contains **no production source change**.

| 12 fresh-context passes per row | Baseline median / p95 | Candidate median / p95 | Warm median, both |
| --- | --- | --- | --- |
| Vite dev | 52.05 / 131.9 ms | 52.50 / 73.6 ms | 0.3 ms |
| Built assets | 56.30 / 77.4 ms | 61.40 / 68.3 ms | 0.3 ms |

The 20% lower median gate was unmet in both modes. Nearest-rank p95 is the maximum with 12 observations; the first Vite baseline pass included dependency re-optimization and a slow resume/module load, and remains in the raw data. There were **zero null cold observations, zero page errors, zero post-click sample fetches, and zero decodes** across 48 passes. All 48 first post batches contained two events. This checks initial control delivery, not worklet consumption or audible notes. Baseline median resume was 13.4 ms dev / 16.25 ms built; voice module 8.5 / 7.4 ms; mixer module 2.45 / 3.55 ms. Post-module-to-first-post was about 26 ms. Overlapping only the mixer module cannot support the requested gain on these samples.

## Verification

- `node tests/perf/cold-start.mjs --out docs/dev/cold-start-baseline-dev.json --runs 12` — 12/12 cold and 12/12 warm observations.
- `node tests/perf/cold-start.mjs --mode built --out docs/dev/cold-start-baseline-built.json --runs 12` — 12/12 cold and warm observations, after a successful `tsc --noEmit` and Vite build.
- The same two probe commands with `cold-start-candidate-dev.json` and `cold-start-candidate-built.json` outputs — 12/12 each, candidate then reverted. Raw JSON is committed in the PR.
- `node node_modules/vitest/vitest.mjs run --project unit tests/session.test.ts tests/session-sample-start.test.ts tests/session-lane-events.test.ts` — 18 passed in 3 files.
- `node node_modules/vitest/vitest.mjs run --project browser tests/browser/audio-context.test.ts tests/browser/mixer-audio.test.ts tests/browser/audio-determinism.test.ts` — 17 passed in 3 files.
- `node node_modules/typescript/bin/tsc --noEmit` — passed.
- `node node_modules/eslint/bin/eslint.js tests/perf/cold-start.mjs` — passed after correcting the new probe's global declaration. Its initial lint run failed on that declaration; the corrected rerun passed.
- `node node_modules/vite/bin/vite.js build` — passed after reverting the candidate; existing ineffective dynamic import warnings for `newProject.ts` and `boot.ts` were emitted.
- `git diff --cached --check` — passed for the evidence commit. Final PR has no changes to production audio files.

## Limits and dependent work

The four timing series ran by condition, not randomized; headless browser and OS variation may explain some differences. Dev used Vite; built used local preview, not deployed assets. No listening, physical loopback, phone, sample-backed Play, or real output latency measurement was done. First port post does not establish audible onset, and the two-event first batch does not prove what reached a speaker. No output sample behavior is changed by the final PR. There is no safe measured production optimization from this slice. P-06 can proceed with its visualizer timing work; the timing slot has been released.
