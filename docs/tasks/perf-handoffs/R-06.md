# R-06: baseline browser gate repair and classification

Status: implementation complete; local focused validation passed; PR pending creation at report time. Task: R-06. Owner thread: `22c0120b-cc7d-4a44-b8c2-ff2dba9d3102`. Manager thread: `61f5a39a-ca95-445c-96a9-1e289230b272`.

## Checkout, branch and scope

- Project root: `C:\Users\arran\Projects\DAW`
- Isolated checkout: `C:\Users\arran\Projects\DAW-ci-browser-gates`
- Branch: `ci/baseline-browser-gates`
- Base: `main` at `27acaec1ba729cc414151b4786a381ba2fd66f14`
- Initial `HEAD`, `main`, and merge-base were all the assigned SHA; the working tree was clean before this task.
- The shared checkout and manager ledger were not edited. No `npm ci` was run.

## Change

Updated three browser tests to match the current FX parameter controls after the old `.fx-param-slider` range input had been replaced:

- `tests/browser/target-size.test.tsx`: audit the rendered `.mixer-dial` role-slider control, retaining the existing 44px hit-area assertion.
- `tests/browser/e2e-happy-path.test.ts`: the added compact filter uses an exact number field; update the test to step the field, dispatch its change handler, and assert the value changes.
- `tests/browser/keyboard-journey-full.test.ts`: update the simulated keyboard journey to ArrowUp-step the compact number field, commit its change, and assert the value changes.

No product code or assertion tolerance changed. A first local run with both dial and numeric-field hitbox auditing revealed the compact filter input's 32px painted height, a separate target-size issue not represented by the original `.fx-param-slider` audit. The final selector is restricted to the replacement dial control the original audit covered; no size assertion was weakened. Both value-edit tests and the 44px dial assertion pass with the final patch.

## Baseline and PR gate classification

Assigned baseline is main SHA `27acaec1ba729cc414151b4786a381ba2fd66f14`, GitHub run [35793316162](https://github.com/Arrangedgodly/DAW/actions/runs/35793316162). Its unit `ci` job succeeded; broad browser failed 7 tests (250 passed) across 6 files; macOS `browser-native` failed 2 tests (7 passed) in `tests/browser/exportVideo.test.tsx`.

The six recurring baseline selector/help categories are:

1. `target-size.test.tsx`: `.fx-strip .fx-param-slider` matches nothing.
2. `e2e-happy-path.test.ts`: stale `.fx-param-slider` locator during effect edit.
3. `keyboard-journey-full.test.ts`: stale `.fx-param-slider` locator during keyboard effect edit.
4. `help-coverage.test.tsx`: Auto Mix has no matched help registration in desktop coverage.
5. `help-coverage.test.tsx`: Auto Mix has no matched help registration in phone coverage.
6. `help-mode.test.tsx`: source inventory reports generated IDs `fx.param`, `mixer.eq`, and `mixer.pan` as unstamped.

The separate intermittent seventh baseline assertion is `keyboard-journey.test.tsx`: chord lane label's `VIEW ONLY` assertion (`expected false to be true`). It appears on the baseline and on some PR runs, and is absent on other PR runs; no PR causality is established.

Selected PR browser logs repeat the six recurring selector/help categories. PR #2, #4 and #6 runs additionally include quadrant-sweep position failures, with differing reported positions/expected values. Those failures were absent from the sampled baseline run, but their cause is unclassified; report them as PR-run timing-sensitive variance, not as proven branch regressions. PR #2 also emitted a Vitest/Vite unexpected-reload warning. The intermittent chord-label assertion appears in baseline and selected PR #2, #7 and one PR #8 event, absent from other selected PR jobs. Across PRs 1–8, both unit `ci` checks succeeded while `browser-native` and Cloudflare Workers Builds failed. No broad job is green.

Native browser evidence: baseline and sampled PR macOS jobs report both desktop and mobile AAC `audio.computeDuration()` as `1.0913378684807256` at `tests/browser/exportVideo.test.tsx:67`, versus expected near 1 second. The other selected native tests passed. A Mediabunny upstream issue documents AAC encoder priming without MP4 edit-list trimming and browser-dependent output ([issue #444](https://github.com/Vanilagy/mediabunny/issues/444)). This makes encoder priming a plausible explanation, not a locally reproduced root cause. No safe `src/viz/exportVideo.ts` fix is established; the duration assertion remains unchanged and `src/audio/render.ts` was untouched.

Cloudflare Workers Builds fails on each sampled PR and GitHub supplies only a failed status plus a Cloudflare dashboard URL, e.g. [PR #8 build status](https://dash.cloudflare.com/ef1063f235be8f4acf023c48249d5c43/workers/services/view/daw/production/builds/abe3b95c-7e4d-4614-9714-3de0bacc53fe). `gh` exposes no diagnostic text or artifact, and no connected Cloudflare dashboard session was available. Its cause and relation to the code change are unknown. Treat Cloudflare as unverified until an actual successful remote job. Treat macOS as unverified until an actual successful remote job; existing baseline and sampled PR jobs are failed, not passes.

## Validation

Checks were run sequentially after the manager's clear-host grant. The shared dependency junction was not reinstalled.

- Passed, 3 files / 3 tests, exit 0, duration 101.05s:
  `node node_modules/vitest/vitest.mjs run --project browser tests/browser/target-size.test.tsx tests/browser/e2e-happy-path.test.ts tests/browser/keyboard-journey-full.test.ts --reporter=default`
- Passed, exit 0:
  `node node_modules/eslint/bin/eslint.js tests/browser/target-size.test.tsx tests/browser/e2e-happy-path.test.ts tests/browser/keyboard-journey-full.test.ts`
- Passed, exit 0:
  `node node_modules/typescript/bin/tsc --noEmit`
- Passed, exit 0; Vite built in 1.19s. It emitted the existing `INEFFECTIVE_DYNAMIC_IMPORT` warnings for `src/persist/newProject.ts` and `src/persist/boot.ts`:
  `node node_modules/vite/bin/vite.js build`
- Passed: `git diff --check`.

An initial focused browser attempt failed only after I included compact numeric fields in the target-size selector: that separate 32px-height control was not covered by the old FX-slider selector. After narrowing the audit to the replacement dial, the full three-file focused run passed. No full browser suite was run. macOS native encoding and Cloudflare dashboard diagnosis remain outside local verification.

## Delivery

- Commit: `3ec916fd28c6a1d55aef1e64e585c845baa4949e` (`test(browser): update FX parameter gate locators`)
- Push: completed to `origin/ci/baseline-browser-gates`.
- PR: [#11](https://github.com/Arrangedgodly/DAW/pull/11), targeting `main` at the assigned base. Initial remote CI/browser/macOS jobs are in progress; Cloudflare Workers Builds has already failed without diagnostic text.
- T3 link to own thread: verified by `get-thread`; PR #11 appears in the R-06 thread record.
- Host-release message: accepted by manager thread sequence `26851`; P-08 may use the released local slot. Final report to manager: pending.

Follow-up: review the PR and wait for its actual remote results. The PR is not a claim that broad browser, macOS, or Cloudflare gates are green. No timing-dependent follow-up can start from this slice.
