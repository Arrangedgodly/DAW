# P-08 — visualizer deformation hotpath handoff

## Status and stack

**Measurement-only PR; no production change.** The one candidate missed the 25% callback p95 reduction gate and posted two fewer notes in one paired window. It was reverted. Branch `perf/viz-deform-hotpath` is based on P-06 `perf/viz-frame-cost` at `eccd6645157203272db8d1d9a523e0a1bd1378fb`. [PR #12](https://github.com/Arrangedgodly/DAW/pull/12) targets `perf/viz-frame-cost` (PR #7), which stacks on `perf/latency-map` (PR #5). The evidence commit is `c1db97ca84e38c8cbddc69ea44c87e90752b543c`; this handoff is added after the PR opened. The PR is open and was mergeable at the checked snapshot. T3 `link-pr` was accepted and `get-thread` lists PR #12 on worker thread `922bcebc-a980-420f-899c-21f44d4a36c6`.

## Work and evidence

P-06's committed clear-host profile had 151 self samples in per-point `deform`, 54 in `drawLayer`, and 17 in `scatter`; these are sampled attribution, not elapsed time. I tried exactly one candidate: select the motion-mode deformation closure once per layer and hoist frame-constant time and pulse expressions. It kept the point formulas' arithmetic order, deterministic grain sequence, geometry and particle counts, lane identity and playback paths. The candidate source blob was `486e5c0fd9373f5193aeb7ce2f020975b7f1e03e` during capture. Its patch and other optional local evidence are preserved outside the PR at `C:\Users\arran\Projects\DAW-perf-viz-deform-local-evidence`.

After R-06 released the host and the manager granted timing, I ran the existing `tests/perf/viz-frame.mjs` sequentially on clean base then candidate. Both exited 0 in about 23 seconds. Conditions were Headless Chromium `151.0.7922.34`, Windows `win32/x64`, `1280 × 720`, Glass Arcade eight-track demo, five grid/visualizer pairs of 1.2-second comparison windows. CPU profiling and tracing occurred after those unprofiled windows. The baseline revision field is `eccd6645157203272db8d1d9a523e0a1bd1378fb`; the candidate field adds `+candidate-486e5c0f`. No build or browser suite ran between timed probes.

```powershell
$env:GIT_COMMIT = (git rev-parse HEAD)
node tests/perf/viz-frame.mjs --out docs/dev/viz-deform-baseline-clear.json
# Apply the candidate patch and verify source blob 486e5c0f.
$env:GIT_COMMIT = 'eccd6645157203272db8d1d9a523e0a1bd1378fb+candidate-486e5c0f'
node tests/perf/viz-frame.mjs --out docs/dev/viz-deform-candidate-clear.json
```

| Five visualizer windows | Base | Candidate |
| --- | ---: | ---: |
| Callback p95, ms | 4.0, 4.1, 5.3, 5.2, 4.3 | 4.0, 4.5, 5.4, 4.4, 4.7 |
| Median of window p95 values | 4.3 ms | 4.5 ms |
| Intervals >=33.4 ms | 2 / 343 (0.58%) | 1 / 339 (0.29%) |
| Posted notes by window | 26, 30, 24, 34, 21 | 26, 28, 24, 34, 21 |

The candidate failed the at-least-25% lower median threshold (required <=3.225 ms at this baseline); its 4.5 ms result is about 4.7% higher. Its interval ratio was lower but one paired window posted 28 rather than 30 notes. A baseline repeat was unnecessary for a result far from the speed gate. The candidate was reverted before final validation. The decisive raw callback, timestamp and note-post histories and both traces are losslessly compressed as `.json.gz` in `docs/dev/`; gzip and JSON integrity checks passed. Uncompressed traces, small CPU profiles, representative PNGs and the candidate patch remain local outside the PR. The representative PNGs are not a pixel comparison because they reflect different song times.

The [analysis](../../dev/viz-deform-hotpath.md) documents methods, files and limits. The final `src/viz/compositionEngine.ts` blob is exactly P-06's `a59cbd340169bdb291110aead434fda8e4537be3`: `git hash-object` and `git rev-parse HEAD:src/viz/compositionEngine.ts` returned the same ID, and `git diff --exit-code -- src/viz/compositionEngine.ts` exited 0. No audio engine, scheduler, FX, schema, manager ledger, test, or production source file is in the PR.

## Sequential validation on the reverted source

- `node node_modules/vitest/vitest.mjs run --project unit tests/viz-composition.test.ts tests/viz-renderer-lifecycle.test.ts tests/viz-pipeline.test.ts tests/viz-timbre.test.ts` — exit 0; 4 files, 90 tests passed.
- `node node_modules/vitest/vitest.mjs run --project browser tests/browser/viz-frame-pixels.test.ts tests/browser/viz-fingerprint.test.ts` — exit 0; 2 files, 3 tests passed. The four pinned eight-lane hashes are `3372253067`, `3672592734`, `2858053307`, `3234583933`; existing fingerprints cover all 24 geometry effects. Candidate pixel parity was not certified because that candidate failed the speed gate and was removed.
- `node node_modules/vitest/vitest.mjs run --project browser tests/browser/frame-budget.test.ts -t "VZ-TH-4"` — exit 0; 1 focused test passed, 11 unrelated tests skipped.
- `node node_modules/typescript/bin/tsc --noEmit` — exit 0.
- `node node_modules/eslint/bin/eslint.js src/viz/compositionEngine.ts tests/perf/viz-frame.mjs tests/browser/viz-frame-pixels.test.ts tests/browser/viz-fingerprint.test.ts` — exit 0.
- `node node_modules/vite/bin/vite.js build` — exit 0; two existing ineffective dynamic-import warnings from `src/persist`/`src/webmcp` imports.
- `git diff --exit-code -- src/viz/compositionEngine.ts`, source blob check, `git diff --cached --check`, and gzip/JSON integrity checks — passed. The browser tests' global setup built the app; commands ran sequentially after the manager's validation grant. `dist` and the shared `node_modules` junction were not committed or reinstalled.

The timing-complete interim report and validation-complete release were both accepted in manager thread `61f5a39a-ca95-445c-96a9-1e289230b272`. The full handoff will be sent there after this report is pushed.

## Limits and next work

Production acceptance is unmet: the candidate neither passed the speed gate nor maintained the same note-post count in every window. The base version retained its fixed frame hashes, 24-geometry fingerprint test and focused frame-budget test. No visual or audio gain is claimed. Headless rAF callbacks are not target-device GPU cost, audio rendering deadlines or physical speaker latency. Event posts show control-side delivery, not worklet consumption or audible output. No phone, loopback, listening or physical-device check was performed. A further draw optimization would need a new measured hypothesis and a new acceptance run; this PR has no dependent production change to start. Remote CI status should be checked during PR review; this handoff records the exact local checks above.
