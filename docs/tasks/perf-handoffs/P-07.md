# P-07 offline WAV render throughput handoff

Status: measurement-only result. The conditional production gate is unmet. No production code changed.

## Scope and result

The isolated checkout is `C:\Users\arran\Projects\DAW-perf-offline-render`, on `perf/offline-render-throughput` from `perf/latency-map` at `a292f3d733cd32e65fbb0dffb20afdd78686c6ed`. The original `DAW` checkout and shared orchestration ledger were not edited. The only files in this PR are an opt-in throughput probe, its raw baseline samples, this handoff, and [the measurement report](../../dev/offline-render-throughput.md).

The manager granted P-07's host timing slot after R-05 released it. The probe ran six serial baseline WAV renders each of Welcome and Glass Arcade. Median native `OfflineAudioContext.startRendering()` time was 839.6 ms for Welcome and 2327.1 ms for Glass Arcade. Median total `renderProjectToBuffer` time was 915.4 and 2440.4 ms. The raw six-pass data and full WAV hashes are in `docs/dev/offline-render-baseline-samples.json`. Each project's six hashes matched. Both outputs were stereo, 44.1 kHz, and retained the expected linear tail frame count.

Source review and the baseline did not identify a material controllable native-render phase in `render.ts`. Setup, ACK/settle, copying, and encoding occur outside the native interval. The remaining native cost includes required voice, FX, mix, serial fan-in, and tail processing. Therefore no candidate was applied or timed. The 15% gate is unmet, and this PR makes no production throughput claim. The baseline must not be described as a live or speaker latency result. P-07 released the timed slot with an interim report to the manager before heavy validation.

## Verification

Completed:

- `node --check tests/perf/offline-render-throughput.mjs` passed.
- `& .\node_modules\.bin\eslint.cmd tests/perf/offline-render-throughput.mjs` passed after adding the `console` global annotation. The first run failed `no-undef` at line 118; that was fixed.
- `& .\node_modules\.bin\vitest.cmd run --project unit tests/render.test.ts tests/exportWav.test.ts tests/wav.test.ts` passed, 3 files and 28 tests.
- `& .\node_modules\.bin\tsc.cmd --noEmit` passed.
- `$env:GIT_COMMIT = (git rev-parse HEAD); node tests/perf/offline-render-throughput.mjs --label baseline --out docs/dev/offline-render-baseline-samples.json` passed in 24.6 seconds. Revision and browser environment are in the raw file.
- `& .\node_modules\.bin\prettier.cmd --check tests/perf/offline-render-throughput.mjs docs/dev/offline-render-throughput.md docs/tasks/perf-handoffs/P-07.md docs/dev/offline-render-baseline-samples.json` passed after formatting the probe and report. The first check found formatting changes in the probe and report; both were formatted.
- `& .\node_modules\.bin\vitest.cmd run --project browser tests/browser/exportWav.test.ts tests/browser/linear-export.test.ts tests/browser/audio-determinism.test.ts tests/browser/render-parity.test.ts tests/browser/render-mix.test.ts tests/browser/render-fingerprint.test.ts tests/browser/sample-voice.test.tsx tests/browser/exportVideo.test.tsx` passed, 8 files and 40 tests. The fingerprint suite printed four environment-pinned drift warnings without failures. It did not establish cross-environment byte parity.
- `& .\node_modules\.bin\tsc.cmd --noEmit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & .\node_modules\.bin\vite.cmd build; exit $LASTEXITCODE` passed after the browser suite. Vite built 282 modules and warned about two ineffective dynamic imports in unrelated persistence modules.
- `git diff --cached --check` passed with all four new files staged.

P-07 sent the validation-complete host release to the manager before R-06's heavy checks. The manager acknowledged it and granted R-06 the next slot.

## Delivery

Branch: `perf/offline-render-throughput`.

Base: `perf/latency-map` at `a292f3d733cd32e65fbb0dffb20afdd78686c6ed`.

Commit and PR: pending delivery.

Worker thread: `b40b8ecc-3252-4ab4-8245-5c229cc3f6f2`.

PR link and manager report: pending.

Issues: no production optimization met the conditional gate. Physical playback, listening, and live render deadlines were outside this offline export slice. Dependent work can use the baseline and phase split to choose a separately scoped native DSP or FX optimization candidate; none is selected here.
