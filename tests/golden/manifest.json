{
  "manifestVersion": 1,
  "env": {
    "node": "v24.8.0",
    "regeneratedVia": "npm run goldens:update (UPDATE_GOLDENS=1 vitest run tests/golden && UPDATE_GOLDENS=1 vitest run --project browser tests/browser/render-fingerprint.test.ts)"
  },
  "goldens": {
    "render/reference-loop-fp-v1": {
      "note": "SHA-256 of the raw Float32 channel bytes of the full-FX reference project offline render (HW-2 canary). ENVIRONMENT-PINNED (Chromium SIMD/libm): mismatch is a console DRIFT warning, never a CI failure. Guards unintended engine/DSP/dependency drift under the same pinned browser.",
      "sha256": "e87ae0ab727cb10b8d20921f7aba8d3db4d106cd8502578d9d7fe0dca40667f9",
      "byteLength": 705600,
      "kind": "render",
      "renderEnv": {
        "playwright": "playwright 1.62.1",
        "chromium": "chromium (MacOS/Google Chrome for Testing)",
        "sampleRate": 44100,
        "loopSamples": 88200
      }
    },
    "wav/encoder-stereo-2frame-v1": {
      "note": "Byte-exact WAV of a synthetic 2-frame stereo buffer through encodeWav16 (MF-4). Pins the hand-rolled 44-byte RIFF header + int16 quantization law (clamp, x32767, round toward +inf); a sibling test also asserts the bytes equal the hand-computed 1991-spec values. Pure TS, hard failure on mismatch.",
      "sha256": "cfdbfd64a0dd69733cdc496fcbf7a56fd8603d9d525a9726d4e5000f45dd0af7",
      "byteLength": 52
    },
    "midi/reference-project-v1": {
      "note": "Byte-exact Type-1 MIDI of the MF-5 reference project (every lane, triad chord, sustain marker, tempo map, ch10 drums, cue markers). Pins the export note-model + midi-file serialization; structure is separately asserted in golden test and parsed back by @tonejs/midi in the browser suite. Pure TS, hard failure on mismatch.",
      "sha256": "130684574a974cfde80b878f6497270a9893ae0197b70b29c0f4f27d6f4e8d75",
      "byteLength": 322
    },
    "wav/reference-export-fp-v1": {
      "note": "SHA-256 of the exported .wav FILE bytes (44-byte header + 88200 stereo int16 frames = 352844 bytes) for the full-FX reference project (HW-3). Complements MF-4's decode-and-assert suite by pinning the exact file bytes. Environment-pinned like the render fp (samples come from Chromium): mismatch is a console DRIFT warning, never a CI failure.",
      "sha256": "3eff577190d4efb400f080f19973351caeb0fbfad4e6f7cdb6c98293eaf0f4e0",
      "byteLength": 352844,
      "kind": "render",
      "renderEnv": {
        "playwright": "playwright 1.62.1",
        "chromium": "chromium (MacOS/Google Chrome for Testing)",
        "sampleRate": 44100,
        "loopSamples": 88200
      }
    },
    "codec/default-project-canonical-v2": {
      "note": "Canonical JSON codec bytes of createDefaultProject() (HW-1). Pins the .bitbounce save format: field order, key naming, no-whitespace canonicalization. Pure TS, byte-stable everywhere; mismatch = hard failure. SC-1: regenerated for schema v2 (pitched patterns = rowDegrees + notes; version 2).",
      "sha256": "93139e556a13e39d3a2747c0a89e6af12d84bb0de7822fb891d059058f40ad14",
      "byteLength": 1632
    },
    "codec/demo-project-canonical-v2": {
      "note": "Canonical JSON codec bytes of createDemoProject() — the WELCOME SONG first-run demo (PX-1). Pins the demo's composition bit-exactly (patterns, chains, cues, FX, transport): any deliberate demo edit shows up as a reviewable manifest diff. Pure TS, byte-stable everywhere; mismatch = hard failure. SC-1: regenerated for schema v2 notes (same composition; lengths = the migration law).",
      "sha256": "76367d53a382f5e9cf24e87f86f9217363b108ab93cb7ee7445b508cde17abef",
      "byteLength": 6887
    },
    "migrate/v1-default-to-v2": {
      "note": "SC-1 migration fixture: canonical v2 bytes of decode(v1 default-project bytes). The v1 text is the v1 cell-row shape; migrating it must equal the shipped v2 default exactly. Pins the migration law; mismatch = hard failure.",
      "sha256": "93139e556a13e39d3a2747c0a89e6af12d84bb0de7822fb891d059058f40ad14",
      "byteLength": 1632
    },
    "migrate/v1-demo-to-v2": {
      "note": "SC-1 migration fixture: canonical v2 bytes of decode(v1 WELCOME SONG bytes). The demo is the only sustain-marker-heavy shipped project — its 6-step-gate chord pads must migrate to 15-step notes. Pins the duration-preserving law; mismatch = hard failure.",
      "sha256": "76367d53a382f5e9cf24e87f86f9217363b108ab93cb7ee7445b508cde17abef",
      "byteLength": 6887
    },
    "migrate/v1-sustain-heavy-to-v2": {
      "note": "SC-1 migration fixture: canonical v2 bytes of decode(hand-authored sustain-heavy v1 neighbor) — sustained runs, lone hits, orphan markers (dropped), a seconds-unit gate (0.25 s at 112 BPM -> 1.75 steps). Pins the quantization corner; mismatch = hard failure.",
      "sha256": "dfd3123147108d5d0ac8115d9f7fd780f45f7cf05028b6460a88b55614d8fd5e",
      "byteLength": 1907
    },
    "wav/reference-export-mix-fp-v1": {
      "note": "SHA-256 of the exported .wav FILE bytes for the full-FX reference project WITH a non-default lane mix (drums muted, lead volume 0.75) — the HW-5 export-mix canary: WAV export applies volume/mute/solo through the render pipeline (the coordinator resolution recorded at LY-1 verification). Environment-pinned like the unmixed export fp (samples come from Chromium): mismatch is a console DRIFT warning, never a CI failure. The hard audio-law proofs (exact-silence windows, solo = complementary mute, volume linearity) live in tests/browser/render-mix.test.ts.",
      "sha256": "403361caf6e1049291b786a34f2300422d0a58a5ff81b7e4b2c4677301ca0fcd",
      "byteLength": 352844,
      "kind": "render",
      "renderEnv": {
        "playwright": "playwright 1.62.1",
        "chromium": "chromium (MacOS/Google Chrome for Testing)",
        "sampleRate": 44100,
        "loopSamples": 88200
      }
    }
  }
}
